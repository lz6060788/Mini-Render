本代码仓库是基于著名的`canvas`绘制引擎`ZRender`（`Echarts`的核心库）而制作的从零开发的过程记录库，主要用于学习的目的。

各章节将按顺序逐步实现与完善一个`mini`版本的`ZRender`，尽管`ZRender`本身已经十分轻量。



#### `demo`效果预览

```bash
cd ./mini-render/章节/

pnpm i

npm run dev
```

